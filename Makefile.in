cmd=${dir}/.docker/run_docker.sh

build:
	${cmd} "echo \"Hello dear friend\""

into:
	${dir}/.docker/into.sh

stop:
	${dir}/.docker/stop.sh

android_studio:
	${cmd} "/opt/android-studio/bin/studio.sh"

